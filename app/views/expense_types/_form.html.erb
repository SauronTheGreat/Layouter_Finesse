<div class="row">
  <div class="span7">
    <%= form_for(@expense_type) do |f| %>
        <% if @expense_type.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(@expense_type.errors.count, "error") %> prohibited this expense_type from being
                saved:</h2>

              <ul>
                <% @expense_type.errors.full_messages.each do |msg| %>
                    <li><%= msg %></li>
                <% end %>
              </ul>
            </div>
        <% end %>

        <div class="clearfix">
          <%= f.label :name %>
          <div class="input">
            <%= f.text_field :name, :class=>"xlarge" %>
          </div>
        </div>

          <div class="input">

                <%= f.submit "Confirm Decision", :class=>"primary btn" %>


        </div>
    <% end %>
  </div>
</div>