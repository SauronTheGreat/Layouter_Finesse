<div class="row">
  <div class="span5" style="margin-left: -20%;margin-top: 10%">
    <%= form_for(@loan_type) do |f| %>
        <% if @loan_type.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(@loan_type.errors.count, "error") %> prohibited this loan_type from being saved:</h2>

              <ul>
                <% @loan_type.errors.full_messages.each do |msg| %>
                    <li><%= msg %></li>
                <% end %>
              </ul>
            </div>
        <% end %>

        <div class="clearfix">
          <%= f.label :name %>
          <div class="input">
            <%= f.text_field :name, :class=>"xlarge" %>
          </div>
        </div>
        <br>

        <div class="row">
          <div class="span2 offset3">


            <%= f.submit "Confirm Decision", :class=>"primary btn" %>

          </div>
        </div>
    <% end %>
  </div>
</div>