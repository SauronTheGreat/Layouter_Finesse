<%= form_for(@round_loan) do |f| %>
  <% if @round_loan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@round_loan.errors.count, "error") %> prohibited this round_loan from being saved:</h2>

      <ul>
      <% @round_loan.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :round_id %><br />
    <%= f.number_field :round_id %>
  </div>
  <div class="field">
    <%= f.label :loan_id %><br />
    <%= f.number_field :loan_id %>
  </div>
  <div class="field">
    <%= f.label :amount %><br />
    <%= f.number_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
