
<%= @investment_type.name %>


<div class="row">
  <div class="span5" style="margin-left: -75px;margin-top: 0">
    <%= form_for(@investment_type) do |f| %>
        <% if @investment_type.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(@investment_type.errors.count, "error") %> prohibited this investment_type from being
                saved:</h2>

              <ul>
                <% @investment_type.errors.full_messages.each do |msg| %>
                    <li><%= msg %></li>
                <% end %>
              </ul>
            </div>
        <% end %>

        <div class="clearfix">

          <div class="input">
            <%= f.text_field :name, :class=>"xlarge" %>
          </div>
        </div>

        <div class="input">

          <%= f.submit "Confirm Decision", :class=>"primary btn" %>


        </div>
    <% end %>
  </div>
</div>
