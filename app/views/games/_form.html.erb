<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :simulation_id %><br />
    <%= f.select :simulation_id ,Simulation.all.collect{|sim|[sim.name,sim.id]}%>
  </div>
  <div class="field">
    <%= f.label :student_group_id %><br />
    <%= f.select :student_group_id ,StudentGroup.all.collect{|sg|[sg.name,sg.id]}%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
