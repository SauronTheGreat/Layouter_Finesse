<%= content_for :main_content do %>
<h1>Listing media_plans</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>

  </tr>

<% @media_plans.each do |media_plan| %>
  <tr>
    <td><%= media_plan.name %></td>
    <td><%= link_to 'Show', media_plan %></td>
    <td><%= link_to 'Edit', edit_media_plan_path(media_plan) %></td>
    <td><%= link_to 'Destroy', media_plan, confirm: 'Are you sure?', method: :delete %></td>
    <td><%= link_to "Register as Expense",register_as_expense_path(:plan_id=>media_plan.id) %></td>

  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Media plan', new_media_plan_path %>
<% end%>