<%= form_for(@hrincentive) do |f| %>
  <% if @hrincentive.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hrincentive.errors.count, "error") %> prohibited this hrincentive from being saved:</h2>

      <ul>
      <% @hrincentive.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
