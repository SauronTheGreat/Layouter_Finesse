<%= content_for :page_header do %>
    Showing <%= @brand.name %>
<% end %>
<div class="row">
  <div class="span7 offset3">
    <p>
      <b>Player:</b>
      <%= @brand.player_id %>
    </p>

    <p>
      <b>Name:</b>
      <%= @brand.name %>
    </p>

    <p>
      <b>Price per unit:</b>
      <%= @brand.price_per_unit %>
    </p>

  </div>
</div>
<%= link_to 'Edit', edit_brand_path(@brand) %> |
<%= link_to 'Back', brands_path(:player_id=>@brand.player_id,:round_id=>ActiveRound.first.round_id) %>

